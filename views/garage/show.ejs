<%- include('../partials/header')%>

    <div class = "item">

        <% if(ditems != null){ %>
            <p><%= ditems.name %></p>
            <p><%= ditems.price %></p>
            <p><%= ditems.description %></p>
            <p><%= ditems.purchased %></p>
            <p><%= ditems.userName %></p>
            <img src="<%= ditems.userAvatar %>">
        <% }else if(ditems == null){ %>
            <p>Sup foo</p>
       <% } %>


        <form action="/garage/<%=ditems._id%>" method = "post">
            <input type="submit" value="Add To Cart">
        </form>
    </div>
    

<%- include('../partials/footer')%>
