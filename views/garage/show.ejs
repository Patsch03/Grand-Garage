<%- include('../partials/header')%>

    <div class = "item">
        <p><%= ditems.name %></p>
        <p><%= ditems.price %></p>
        <p><%= ditems.description %></p>
        <p><%= ditems.purchased %></p>
        <form action="/garage/<%=ditems._id%>" method = "post">
            <input type="submit" value="Add To Cart">
        </form>
    </div>
    

<%- include('../partials/footer')%>
